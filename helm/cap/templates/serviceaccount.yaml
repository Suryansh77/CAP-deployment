apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cap.serviceAccountName" . }}
  labels:
    {{- include "cap.labels" . | nindent 4 }}
automountServiceAccountToken: {{ .Values.serviceAccount.automount }}